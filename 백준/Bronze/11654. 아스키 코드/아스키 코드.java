import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        char ascii = scanner.next().charAt(0);
        int ascii_num = (int)ascii;
        System.out.println(ascii_num);
    }
}